# api-gateway/src/main/resources/bootstrap.yml

spring:
  application:
    name: api-gateway
  config:
    # Connect to the Config Server (assuming it's running on 8888)
    import: "configserver:http://localhost:8888"

server:
  # This property will be overridden by the config fetched from the server (9090)
  port: 9090