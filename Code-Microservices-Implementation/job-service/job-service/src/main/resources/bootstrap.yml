# job-service/src/main/resources/bootstrap.yml

spring:
  application:
    name: job-service
  config:
    # Use the import property to specify the config server URI
    import: "configserver:http://localhost:8888"